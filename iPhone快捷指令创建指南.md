# 📱 iPhone 端创建「玉树4日批量导入提醒事项」快捷指令

## ⚠ 关于 .shortcut 文件
Apple 自 iOS 13 起要求所有 .shortcut 文件必须经过 Apple 服务器签名才能直接导入。
Linux 沙箱生成的未签名文件 **无法直接在 iPhone 上点击导入**，会提示「无法打开」。

下面两种方案任选其一：

---

## 方案 A：在 iPhone 上手动创建快捷指令（推荐，5 分钟搞定）

### 第 0 步：先把清单文本复制到剪贴板
1. iPhone Safari 打开：
   https://raw.githubusercontent.com/chenzongli1984-star/qinghai-yushu-travel-map/main/玉树iPhone待办清单_优化版.txt
2. 长按正文 → **全选** → **拷贝**（已复制到剪贴板 ✅）

### 第 1 步：打开「快捷指令」APP
- iPhone 自带「快捷指令」APP（Shortcuts）
- 找不到？App Store 搜索「快捷指令」免费下载

### 第 2 步：新建快捷指令
1. 打开「快捷指令」APP
2. 右上角点 **「+」** 新建快捷指令
3. 起名「玉树4日批量导入」(顶部「新建快捷指令」标题处点击重命名)

### 第 3 步：添加 4 个操作

#### 操作 1：获取剪贴板
1. 底部搜索栏输入「**剪贴板**」
2. 点击「**获取剪贴板**」操作添加到流程

#### 操作 2：拆分文本（按换行）
1. 搜索栏输入「**拆分文本**」
2. 点击「**拆分文本**」添加
3. 操作内显示：「拆分 *剪贴板* 按新行」
   - 第一个输入框应为「**剪贴板**」（自动引用上一步输出，如显示「输入」则点击改为「剪贴板」）
   - 中间选项点选「**新行**」（默认即是）

#### 操作 3：重复每个项目（Repeat with Each）
1. 搜索栏输入「**重复**」或「**Repeat**」
2. 点击「**重复每个项目**」(Repeat with Each) 添加
   - 内部显示「重复 *拆分文本* 每个 项目」
   - 输入应为「**拆分文本**」(自动引用，如不是则点击改为「拆分文本」)
3. 此操作展开后会显示「Repeat Result」循环变量

#### 操作 4：创建提醒事项
1. 在「重复」操作**内部**（缩进位置）添加操作
2. 搜索栏输入「**提醒事项**」或「**新建提醒事项**」
3. 点击「**新建提醒事项**」(Add New Reminder) 添加
4. 标题输入框点击 → 选「**Repeat Result**」（循环变量）
   - 默认可能是「问询」或空，点击改为「Repeat Result」
5. 可选：展开「显示更多」→ 在「清单」处选择「**玉树4日**」清单（需先在「提醒事项」APP 创建该清单）

### 第 4 步：最终流程结构

```
1. 获取剪贴板
2. 拆分 [剪贴板] 按 [新行]
3. 重复每个 [拆分文本]
   ↳ 新建提醒事项
      标题: [Repeat Result]
      清单: 玉树4日 (可选)
4. 结束重复
```

### 第 5 步：测试运行
1. 确保剪贴板里是清单文本（第 0 步已复制）
2. 点底部「▶ 运行」按钮
3. 等 5-10 秒（129 条任务创建中）
4. 打开「提醒事项」APP → 进入「玉树4日」清单
5. ✅ 129 条任务一键全部导入完成！

### 第 6 步：保存并加入主屏幕（可选）
1. 点右上角「完成」保存快捷指令
2. 长按快捷指令 → 「分享」→ 「添加到主屏幕」
3. 以后任何多行文本都可复制后点此图标批量导入

---

## 方案 B：在 Mac 上打开 .shortcut 文件（如有 Mac）

1. Mac 下载文件：`玉树4日_批量导入提醒事项.shortcut`
   (https://raw.githubusercontent.com/chenzongli1984-star/qinghai-yushu-travel-map/main/玉树4日_批量导入提醒事项.shortcut)
2. 双击 .shortcut 文件 → 自动在「Shortcuts」APP 打开
3. macOS Shortcuts 接受未签名文件
4. 点「添加快捷指令」导入
5. 通过 iCloud 自动同步到 iPhone（开启 iCloud 同步快捷指令）

---

## 🎯 使用方法

每次想批量导入清单到提醒事项：
1. 在 Safari 或备忘录里**复制**多行清单文本
2. 打开「快捷指令」APP → 运行「玉树4日批量导入」
3. 自动按行拆分创建提醒事项到指定清单

## 📌 适用场景
- 玉树4日清单（已生成，129 行）
- 任何多行文本（购物清单、读书清单、待办清单...）
- 一次复制粘贴，秒级批量导入

## 🔧 故障排查

| 问题 | 解决 |
|---|---|
| 「无法导入」 | 未签名文件，必须按方案 A 手动创建 |
| 提醒事项全部进默认清单 | 在「新建提醒事项」操作里指定清单名 |
| 标题显示「Repeat Result」字面值 | 标题框需点击切换为变量模式（蓝色 = 变量） |
| 运行后没创建任务 | 检查剪贴板是否有内容，再点运行 |
| 任务只有 1 条 | 拆分文本没按换行，确认分隔符选「新行」 |
